# McLarens Analytics Frontend Environment Variables
# Copy this file to .env.local and update with your values
# Note: Variables prefixed with NEXT_PUBLIC_ are exposed to the browser

# ============================================================================
# NEXTAUTH CONFIGURATION
# ============================================================================
# The URL where your Next.js app is running
NEXTAUTH_URL=http://localhost:3000

# Generate a random secret key (minimum 32 characters)
# Run: openssl rand -base64 32
# Or: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET=local-nextauth-secret-min-32-characters-random-string

# ============================================================================
# AUTHENTICATION MODE
# ============================================================================
# Must match the backend AUTH_MODE setting
# Options: dev | entra
NEXT_PUBLIC_AUTH_MODE=dev

# ============================================================================
# MICROSOFT ENTRA ID (Azure AD) - Only needed if AUTH_MODE=entra
# ============================================================================
# Get these values from Azure Portal > App Registrations
# Leave empty for local development with AUTH_MODE=dev
AZURE_AD_CLIENT_ID=
AZURE_AD_CLIENT_SECRET=
AZURE_AD_TENANT_ID=

# ============================================================================
# BACKEND API CONFIGURATION
# ============================================================================
# Backend API base URL (exposed to browser)
NEXT_PUBLIC_API_URL=http://localhost:8000

# GraphQL endpoint (exposed to browser)
NEXT_PUBLIC_GRAPHQL_ENDPOINT=http://localhost:8000/graphql

# Note: With Next.js rewrites configured in next.config.js,
# the frontend can also call /api/* which proxies to the backend
# This eliminates CORS issues during development

# ============================================================================
# OPTIONAL: BACKEND URL FOR SERVER-SIDE REQUESTS
# ============================================================================
# Used by Next.js rewrites in next.config.js
# Default: http://localhost:8000 (local development)
# Docker: http://backend:8000 (container name)
# BACKEND_URL=http://localhost:8000

# ============================================================================
# ANALYTICS & MONITORING (Optional)
# ============================================================================
# Add analytics/monitoring service keys here if needed
# NEXT_PUBLIC_GA_TRACKING_ID=
# NEXT_PUBLIC_SENTRY_DSN=
