# GraphQL Contracts

Shared GraphQL schema types and contracts between frontend and backend.

## Usage

These types are generated from the backend schema and can be used in the frontend.

```bash
npm run codegen
```
