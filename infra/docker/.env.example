# ==========================================
# McLarens Analytics - Docker Environment variables
# Copy this file to .env in the same directory
# ==========================================

# ─── Auth Strategy ───
# 'dev' = Local development with mock login (no Azure required)
# 'entra' = Microsoft Entra ID (Azure AD) login required
AUTH_MODE=dev

# ─── Microsoft Entra ID (Required if AUTH_MODE=entra) ───
# Get these from Azure Portal > App Registrations
AZURE_AD_CLIENT_ID=
AZURE_AD_TENANT_ID=
AZURE_AD_CLIENT_SECRET=

# ─── NextAuth Configuration ───
# URL of the frontend (browser-accessible)
NEXTAUTH_URL=http://localhost:3000

# Secret key for encrypting sessions
# Run `openssl rand -base64 32` to generate
NEXTAUTH_SECRET=changeme_dev_secret_1234567890

# ─── Database Credentials ───
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=maclarens_analytics

# ─── App Users (for seeding/dev login) ───
ADMIN_PASSWORD=admin123
DIRECTOR_PASSWORD=director123
